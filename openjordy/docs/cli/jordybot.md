---
summary: "CLI reference for `openjordy jordybot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `openjordy jordybot ...`
  - You need migration guidance to current commands
title: "jordybot"
---

# `openjordy jordybot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `openjordy jordybot qr` (same behavior as [`openjordy qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `openjordy jordybot qr` -> `openjordy qr`
